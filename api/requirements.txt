# Vercel Python serverless dependencies
# Note: playwright and celery removed — not compatible with serverless runtime

# Web framework
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.20
mangum==0.19.0

# Database
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0

# Settings
pydantic==2.10.3
pydantic-settings==2.7.0

# HTTP client
httpx==0.28.1

# AI
anthropic==0.40.0

# Scraping (Apify only — Playwright excluded: too large for serverless)
apify-client==1.9.2

# Google Sheets
google-auth==2.37.0
google-auth-oauthlib==1.2.1
gspread==6.1.4

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utils
python-dotenv==1.0.1
tenacity==9.0.0
